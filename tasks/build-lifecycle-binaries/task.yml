platform: linux
image_resource:
  type: docker-image
  source:
    repository: golang
    tag: "1.11"

inputs:
  - name: lifecycle-repo
  - name: ci
  - name: lifecycle-image-version
  - name: lifecycle-build-version

outputs:
  - name: lifecycle-binaries

run:
  path: ci/tasks/build-lifecycle-binaries/run
