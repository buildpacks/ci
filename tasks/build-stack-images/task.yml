platform: linux
image_resource:
  type: docker-image
  source:
    repository: concourse/docker-image-resource

inputs:
  - name: lifecycle-repo
  - name: ci
  - name: lifecycle-image-version
  - name: lifecycle-build-version

outputs:
  - name: built-lifecycle-images
  - name: built-lifecycle-version

run:
  path: ci/tasks/build-stack-images/run
