#!/usr/bin/env bash

set -o errexit
set -o nounset
set -o pipefail

echo Moving binaries

pushd lifeyclce-binaries
 tar xzf ../lifecycle/lifecycle-*.tar --strip-components 1
popd

echo lifecycle-binaries after moves:

ls -al lifecycle-binaries

echo Done.
