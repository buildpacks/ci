#!/usr/bin/env bash

source ci/scripts/common.sh

set -o errexit
set -o nounset
set -o pipefail

set -x

cd pack-repo

docker load -i golang-image/image
docker tag "$(cat golang-image/image-id)" "golang"

docker_run golang go test -mod=vendor -v -count=1 -parallel=1 -timeout=0 ./...
